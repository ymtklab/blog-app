extends layout

block content
  div.bg-light.p-3.p-sm-5.my-4.rounded
    h1.display-4 ブログ
    p.lead ささいなことを書くブログ
    p.text-danger ※個人情報や重要事項は書き込まないでください
  p.text-end #{login.username} でログイン中
  h3.my-3 ブログ一覧
  table.table 
    tr 
      th ブログタイトル
      th 投稿者
    each blog in blogs 
      tr 
        td 
          a(href=`/blogs/${blog.blogId}`) #{blog.blogTitle}
        td #{blog.user.username}